# GameFleet – guide for AI assistants and contributors

GameFleet is a small self-hosted dashboard for game servers: a FastAPI backend queries each server with its
native protocol and a SvelteKit SPA shows status, players and per-game details. Read this file before changing
anything; keep it current when the architecture or the rules below change.

## Architecture

```
backend/src/gamefleet_backend/
  main.py                 FastAPI app; version from pyproject.toml; auth dependency on /api/servers and /api/docker; starts discovery loop
  auth.py                 users from GAMEFLEET_USERS (plain or scrypt hash), HMAC bearer tokens, `current_user` dependency,
                          `python -m gamefleet_backend.auth hash` CLI. No users configured = auth disabled (warning logged)
  api/                    routers: auth (login, me), servers (CRUD, live info, host stats, power), docker (status, discovered, import, ignored), games (artwork, public)
  services/               live_server_info (fan-out per protocol), game_asset (artwork cache),
                          docker_service (async docker-py wrapper: list, stats, du, power; caches per container),
                          docker_discovery_service (detect(): labels > image catalog > keywords > ports; import; ignore; background loop)
  lib/query/<protocol>.py one module per protocol: steam (A2S), minecraft, factorio (RCON), satisfactory, ark_ase, ark_asa (EOS)
  models/game_server_type.py  GameServerType enum + GAME_CATALOG (GameSpec: protocol, default ports, query-port rule, label)
  models/container_catalog.py IMAGE_CATALOG (image repo -> game, data/world paths, RCON password env/file), KEYWORDS, LABELS
  models/container_info.py    ContainerInfo, DetectedGame, DiscoveredContainer, HostStats, DockerStatus
  models/server_info.py   live-info models: discriminated union on `kind` (base/steam/minecraft/factorio/satisfactory/ark)
  db/session.py           engine, init_db(): create_all + idempotent LEGACY_SCHEMA_UPGRADES (this IS the migration system)
  db/models/game_server.py  GameServer table (+ source/container_name/data_path/world_path), IgnoredContainer, GameServerPublic
frontend/src/
  lib/api/Api.ts          GENERATED by `make swagger` (swagger-typescript-api) – never edit by hand
  lib/api/ApiService.ts   client singleton; adds the bearer token, clears it on 401
  lib/api/types.ts        LiveServerInfo derived from the generated client
  lib/auth.svelte.ts      session store (token in localStorage), checkSession/login/logout
  lib/live.svelte.ts      live-info + host-stats store, refreshed every 30 s, cached in sessionStorage across navigation
  lib/gameinfo.ts         per-kind capabilities, overview tiles, property chips, byte/uptime formatting
  lib/games.ts            display labels / artwork URLs per game
  lib/components/         UI components; routes/login, routes/main (dashboard + DiscoveredContainers), routes/server/[id] (detail + HostPanel)
  routes/+layout.ts       ssr = false; awaits checkSession() so no page renders before the session is known
  app.css                 Tailwind v4 (CSS-first: @import 'tailwindcss', @custom-variant dark, design tokens). No tailwind.config.js.
```

Key decisions (do not undo without a reason):

- **Schema changes go into `LEGACY_SCHEMA_UPGRADES`** as idempotent SQL. Alembic is configured but has no versions and is
  not run anywhere; do not add migrations. The `game` column is `VARCHAR(50)`, not a Postgres enum, so new game types need no DB change.
- **`GameServerPublic.from_server` validates from attributes** because table rows hold `game` as text; passing
  `model_dump()` output caused Pydantic serializer warnings.
- **Live info is a discriminated union on `kind`.** New game-specific fields go into a `*ServerInfo` subclass; the frontend
  narrows on `kind`.
- **Frontend is a static SPA** (`adapter-static`, `fallback: index.html`). Links use `resolve()` from `$app/paths`.
- **Backend container uses host networking** so it can reach LAN game servers and any local Postgres; the Docker socket is
  mounted so it can discover and control game-server containers (`DOCKER_SERVER_ADDRESS` is where they are queried).
- **Auth is a stateless HMAC bearer token** (no session table, no extra dependency); the SPA keeps it in localStorage because
  frontend and API are different origins. `/api/games` (artwork via `<img>`) stays public on purpose.
- **Docker-linked servers are keyed by container name**, not id (compose recreates containers with the same name).
  `detect()` order is labels > IMAGE_CATALOG > keywords > unique default port; only label/image matches are auto-imported.
  Deleting a docker server puts its name in `ignoredcontainer` so discovery does not re-import it.
- **Host stats are request-driven and cached** (10 s for one-shot `stats`, 5 min for `du`); CPU% is the delta between two
  consecutive one-shot samples, so the first request after a start reports null. No background stats loop.
- **Backups/rollback (planned, not implemented):** build on `data_path`/`world_path` and `ContainerMount` (volume name +
  destination) recorded per server, using stop -> snapshot/restore of the mount -> start. Keep those fields populated.
- Prettier is intentionally not applied repo-wide. Do not reformat files you are not otherwise changing.

## Adding a game

1. Add the enum value and a `GameSpec` to `GAME_CATALOG` in `models/game_server_type.py`.
2. Steam games: add the app id to `STEAM_APP_IDS` in `services/game_asset_service.py` (artwork). Others: explicit image URLs there.
3. Non-A2S games: add `lib/query/<game>.py` returning one of the `*ServerInfo` models and wire it in `live_server_info_service.py`.
4. Popular Docker images for the game go into `IMAGE_CATALOG` (`models/container_catalog.py`) with data/world paths and RCON password source; a keyword into `KEYWORDS`.
5. Add the label in `frontend/src/lib/games.ts`; if the game has a new `kind`, extend `CAPABILITIES`, `overviewTiles` and `serverProperties` in `lib/gameinfo.ts`. Run `make swagger`, commit the regenerated `Api.ts`.

## Versioning and commits

- The project has **one version**, kept identical in `backend/pyproject.toml` and `frontend/package.json` (currently 0.6.0).
  The API reports it, `make release` tags images with it.
- **Bump the version in the same commit as the change** when the commit is user-visible:
  patch (0.5.x) for fixes and dependency updates, minor (0.x.0) for features or UI changes, major for breaking API/DB changes.
  Pure docs/tooling commits do not need a bump.
- After changing `pyproject.toml` run `cd backend && uv lock` and commit `uv.lock`; after changing `package.json` commit `pnpm-lock.yaml`.
- Commit messages: imperative, one line summary (see `git log`), body only when the why is not obvious.
- Never commit `.env`, `backend/data/`, `build/` or generated caches (see `.gitignore`).

## Toolchain (checked September 2026)

| Piece | Version | Notes |
|-------|---------|-------|
| Python | 3.14 (`.python-version`, `python:3.14-slim`) | 3.15 is not released yet |
| uv | pinned in `backend/Dockerfile` (`ghcr.io/astral-sh/uv:<ver>`) | bump the pin with the local `uv --version` |
| Node | 24 LTS (`node:24-alpine`) | Node 26 becomes LTS in October 2026; switch then |
| pnpm | pinned in `package.json` `packageManager`, used via corepack | keep Docker and local in sync |
| TypeScript | 6.x, **not 7** | svelte-check accepts `^5 \|\| ^6`, typescript-eslint `<6.1`; revisit when both support 7 |
| python-dotenv | latest via `[tool.uv] override-dependencies` | `satisfactory-api-client` pins `~=1.0.1` but never imports dotenv |
| ESLint | flat config, `includeIgnoreFile` from `eslint/config` reading the **root** `.gitignore` | `@eslint/compat` is no longer needed |

Upgrade routine: `pnpm outdated` / `uv tree --outdated --depth 1`, read peer ranges before major bumps, then run the checks below.

## Verifying changes

```bash
make check            # svelte-check + eslint + backend import check
cd frontend && pnpm build
make up               # or: VERSION=x docker compose build   (base images, lockfiles)
```

Backend smoke test on a spare port: `cd backend && GAMEFLEET_USERS=admin:secret uv run uvicorn gamefleet_backend.main:app --port 8010`,
`POST /api/auth/login`, then `GET /api/servers`, `/api/servers/live-info`, `/api/docker/discovered`, `/api/servers/host-stats`
with the bearer token. The dev database is the `gamefleet-postgres-dev` container (`db-dev/`). The frontend dev server can
target it with `GAMEFLEET_API_URL=http://localhost:8010 pnpm dev --port 3010`. Pages behind the login can be screenshotted
with puppeteer-core driving Brave (log in through the form first); plain headless screenshots only reach `/login`.

Headless screenshots: `brave --headless=new --disable-gpu --no-sandbox --hide-scrollbars --window-size=1440,1100
--virtual-time-budget=60000 --screenshot=out.png "http://localhost:3000/main?theme=light"` (`?theme=` is a deliberate override in `app.html`).

## Findings from protocol testing

- Public Minecraft (gommehd.net), Bedrock (geo.hivebedrock.network), 7 Days to Die, TF2 and Garry's Mod servers answer their
  queries; a local `factoriotools/factorio:stable` container (`gf-factorio`, RCON password in `/factorio/config/rconpw`) covers Factorio.
- ARK: SA official/Nitrado servers are resolved through ARK's CDN server lists plus the EOS `sessions/{id}` endpoint
  (the older `matchmaking/filter` call returns 403). Private ASA servers need RCON. Official ASE hosts do not answer A2S.
- Satisfactory could not be verified: the known public API server is down and `steamcmd +login anonymous` can no longer
  download the dedicated server. The module runs with `skip_ssl_verification=True` and is untested against a live server.
- BattleMetrics needs a paid API plan and the Steam master server hostname no longer resolves; do not build on either.

## Working rules for AI assistants

- Read the relevant module before editing; keep diffs focused on the request and do not touch unrelated formatting.
- Run `make check` (and `pnpm build` for frontend changes) before declaring work done; report failures verbatim.
- Regenerate and commit `Api.ts` whenever backend API models change.
- Prefer editing `LEGACY_SCHEMA_UPGRADES`, `GAME_CATALOG` and existing components over introducing new layers.
- Ask before anything destructive (dropping volumes, `git push --force`, deleting data) and before pushing images.
- Never put secrets in code, compose files or this document; RCON passwords stay in the database and `.env`.
- Update this file and the README when you change architecture, tooling versions or the make targets.
